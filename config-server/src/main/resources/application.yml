server:
  port: 8001
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/geemu/spring-cloud/  # 配置git仓库的地址
          searchPaths: config-repo                    # git仓库地址下的相对地址，可以配置多个，用,分割。
          username: geemu                              # git仓库的账号
          password: Boy.cfm*#520.                      # git仓库的密码
  http:
    encoding:
      force: true